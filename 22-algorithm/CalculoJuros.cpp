/*#include <iostream>

using namespace std;

int main() {

	float j_capital, parcela, j_periodo, larb;
	
	cout << "Declare o valor da compra: ";
	cin >> j_capital;
	cout << "Declare o numero de parcelas: ";
	cin >> j_periodo;
	
	parcela = j_capital / j_periodo;
	
	if(parcela >= 300)
	{
		larb = parcela + (parcela * 0.07);
		cout << "Voce pagara " << larb << " em " << j_periodo << " parcelas." << endl;
	}
	else
	{
		larb = parcela + (parcela * 0.04);
		cout << "Voce pagara " << larb << " em " << j_periodo << " parcelas." << endl;
	}
	
	return 0;
}
*/
